apiVersion: v1
kind: Pod
metadata:
  name: test-cosign
  namespace: demo
  labels:
    owner: huan
spec:
  securityContext:
    runAsNonRoot: true
    runAsUser: 1000
  containers:
  - name: app
    image: ghcr.io/huynhhuan/w10-api:0.0.4
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
